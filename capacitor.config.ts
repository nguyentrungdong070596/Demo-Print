import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'demoprint.com.vn',
  appName: 'DemoPrint1',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
